# 2026-08-29: Python lint for harness/ (.attest/hooks/ruff/hook.sh). The
# default rule set only (pycodestyle subset + pyflakes) - deliberately
# narrower than ruff's catalog; widen by measurement against real findings.
target-version = "py311"
line-length = 100

[lint]
select = ["E4", "E7", "E9", "F"]
